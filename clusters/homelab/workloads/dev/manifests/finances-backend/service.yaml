apiVersion: v1
kind: Service
metadata:
  name: finances-backend
  namespace: dev-apps
  labels:
    app.kubernetes.io/name: finances-backend
    homelab.io/environment: dev
spec:
  selector:
    app.kubernetes.io/name: finances-backend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
