# Components

Espaco reservado para componentes reutilizaveis entre clusters.

Diretriz:
- so mover itens para `components/` quando houver reutilizacao real
- evitar abstrair cedo demais durante a primeira migracao